# BRIGADIR — Рабочий план (внутренний инструмент)

**Это актуальный план работ.** Продукт строится как некоммерческий внутренний инструмент для своей команды. Полный roadmap продукта ([roadmap.md](roadmap.md)) и разделы Phase 2+ в [spec.md](spec.md) — справочный материал «на вырост»; при противоречии приоритет у этого файла.

## Принятые решения (2026-07-10)

1. **Подключение Jira — API token, без OAuth.** В форме создания workspace пользователь вставляет URL сайта + email + API-токен (создаётся на id.atlassian.com); система сразу валидирует его `GET /myself` и читает проект. Храним `expires_at` (токен живёт ≤ 1 года) и предупреждаем заранее. OAuth 3LO с редиректом — возможное будущее улучшение, не в плане.
2. **UI для workspace и агентов — с самого начала** (перенесено из Phase 2 в Phase 1): агентов будет создавать команда, а не один человек. Флоу: создать workspace (визард с подключением Jira, переподключение — в настройках) → внутри workspace список агентов, «+» создаёт агента (имя, инструкция, executor/модель, триггер-статус, целевые статусы).
3. **В Jira пишет только система.** Агент никогда сам не двигает карточку и не комментирует — он репортит через тулзы (`report_progress` / `request_human` / `complete_task`), а BRIGADIR переводит статусы, пишет комменты и создаёт задачи людям. Это правило зашито в обёртку инструкций.
4. **UI-библиотека — Element Plus** (Vue 3 + Vite + Pinia + TanStack Query): самый полный набор admin-компонентов под наши экраны и самая предсказуемая для агентной генерации кода. **Своей kanban-доски НЕТ** (решение 2026-07-11): Jira остаётся единственной доской — рисовать её read-only копию дорого и бессмысленно. Вместо неё вкладка **Runs** внутри workspace: список прогонов с фильтрами по агенту/статусу и поиском по ключу тикета.
5. **Workspace привязывается к Jira-борде, скоуп зависит от её типа** (решение 2026-07-11). При создании workspace указывается board id (или URL борды); система через Agile API (`GET /rest/agile/1.0/board/{id}`) валидирует борду и определяет тип. **Kanban** → в скоупе все задачи борды. **Scrum** → в скоупе только задачи **активного спринта** (`sprint in openSprints()`); нет активного спринта — поллер вхолостую, триггеры не срабатывают. Вход тикета в скоуп (старт спринта, добавление в активный спринт) эквивалентен смене статуса: если тикет вошёл в скоуп уже находясь в триггер-статусе агента — агент запускается. Старт нового спринта не трогает `updated` у задач → поллер отслеживает id активного спринта и при его смене делает полный рескан спринта в обход high-water mark.

Модель исполнения (подтверждена): один прогон агента = один процесс `claude -p` от начала до конца (внутренний цикл агент крутит сам, «итерации» снаружи не нужны); цепочка агентов — через статусы Jira (агент A завершился → система перевела тикет → новый статус триггерит агента B); агенты друг друга не запускают.

## Что вырезано из полного плана

- ~~Executor'ы `anthropic_api` и `deepseek_api` — абстракция `AgentExecutor` остаётся в коде, реализации добавим при необходимости.~~ **Возвращены в план (реш. 2026-07-16)** — итерации 12–13 ниже. Стартуем с `claude_cli` (+ опционально `claude_routines`).
- OAuth 3LO, динамические вебхуки, Forge — токен + поллинг (+ опционально один системный вебхук, если есть админ-права).
- Песочницы (Docker/gVisor), секрет-брокер с egress-прокси — доверенная команда: bare process + git worktree + env-санация.
- Multi-tenant, users/RBAC/SSO, биллинг, аудит, Marketplace — не нужны. Доступ к дашборду — один общий bearer-токен.
- Компилятор behavior→wrapper — один захардкоженный шаблон обёртки; behavior-опции добавим, если реально понадобятся.

## Итерации

Ритм работы: итерация = 0.5–2 дня, размер PR. Каждая: plan mode → одобрение плана (≈5 мин) → автономная реализация с тестами → чекпоинт по DoD. Журнал прогресса — `docs/progress.md` (ведётся автоматически с первой итерации).

| # | Итерация | Содержание | Участие пользователя |
|---|---|---|---|
| 1 | Скелет | Монорепо (backend/worker/contracts), Postgres-миграции, BullMQ-очереди, mock-executor, тесты пайплайна | Одобрить план, взглянуть на схему БД |
| 2 | Jira-ядро | JiraModule (token auth, rate-limiter, per-issue write queue, transition discovery, ADF), поллер `/search/jql` + high-water mark, машина статусов на mock'е | Тестовый Jira-проект + бот-аккаунт + токен |
| 3 | claude_cli | Executor: spawn `claude -p` (stream-json, `--json-schema`, лимиты, kill process group), env-санация, rate-limit-обработка | 15 мин: первый живой прогон на своей подписке |
| 4 | Callback + отчёты | MCP-сервер (3 тулзы), run JWT, ReportSchema, Stop-hook, скраббер секретов; обёртка обогащается контекстом фичи (эпик + linked issues) | Одобрить план |
| 5 | UI: workspace и агенты | Визард workspace (URL+email+токен, валидация `/myself`, чтение проекта, `expires_at`+алерт), CRUD агентов, переподключение Jira в настройках | Потыкать флоу создания |
| 6 | UI: Runs и human-queue | Вкладка Runs в workspace — список прогонов (фильтры: агент, статус прогона; поиск по ключу тикета; live-обновление через SSE), карточка прогона (чеклисты ✅/❌, таймлайн событий, стоимость, ссылка на Jira-тикет), страница Tasks с разрешением задач; **+ executors API/CRUD** (долг итерации 5: agent-форма не может создать executor — свежий workspace из визарда требует agents.yaml для сида executor'а); **+ мульти-workspace worker** (долг: reconcile/поллер обрабатывают только `workspaces LIMIT 1` — второй workspace не поллится вообще; дашборд-пути уже переведены на per-workspace `JiraClientFactory.forWorkspace` фиксом 2026-07-12, worker-петля по всем enabled-workspace — сюда) | Потыкать UI; критерий: боли №2 и №3 закрыты |
| 7 | UI: workspace-табы | Убрать кнопки Agents/Runs из списка workspace; клик по строке workspace → страница workspace с общим компонентом табов (Agents \| Runs), переключение табов меняет список под ними; прямые роуты `/workspaces/:id/agents` и `/workspaces/:id/runs` остаются (deep-links из карточек/очереди не ломаются) | Потыкать навигацию (UI-итерация, реш. 2026-07-13) |
| 8 | UI: Settings-таб | Третий таб **Settings** на странице workspace (реш. 2026-07-13): readonly-вид данных Jira-подключения и workspace-конфига как структурированные данные (**el-descriptions**, НЕ формы с disabled-полями), у каждого блока кнопка Edit → модалка с формой; executors-админка остаётся секцией таба; роут `/workspaces/:id/settings` резолвится в таб (deep-links живы); кнопка Settings из списка ведёт в таб; отдельная страница настроек упраздняется; одно аддитивное расширение WorkspaceResponse (bot email, branch prefix, scope_jql — чекпоинт 2026-07-13, заодно чинит затирание сохранённого префикса дефолтом 'feat') | Потыкать; финализировать состав readonly-блоков |
| 9 | UI: icon-sidebar | Меню из header переезжает в узкий левый sidebar (~70px): пункты — только иконки без подписей (библиотека **lucide**, пакет `lucide-vue-next`), тултип с названием при hover; бейдж открытых human-tasks переезжает на иконку очереди; Sign out — вниз sidebar'а | Потыкать навигацию (UI-итерация, реш. 2026-07-13) |
| 10 | Platform Settings + именованные профили раннеров | Executor переведён с workspace-скоупа на ПЛАТФОРМЕННЫЙ и переосмыслен как ИМЕНОВАННЫЙ ПРОФИЛЬ РАННЕРА (выполнено вне spec-kit, решения 2026-07-13/14): профиль = транспорт (type, реестр в коде) + model + max_turns + max_parallel_runs + опциональный API-ключ + enabled; агент — чистая роль, привязан через `executors.executor_id`, СВОЕЙ модели не имеет (поле Model убрано из AgentForm, legacy `behavior.model` игнорируется). Миграция 0003 (`executors` без `workspace_id`, глобальный UNIQUE(name), `concurrency_limit`→`max_parallel_runs`), глобальный `/api/executors` (+write-only `api_key`, `has_api_key` в ответах; ключ — AES-256-GCM в `executors.secrets`, задан → спаун с `ANTHROPIC_API_KEY`, нет → подписка хоста), `repository` переехал в `agents.behavior.repository`, сид при создании workspace заменён глобальным type-scoped backfill'ом; per-profile gate в воркере (max_parallel_runs = кап одновременных прогонов профиля; concurrency очереди типа = сумма по enabled-профилям); в UI — страница Settings (шестерёнка, `/settings/executors`, колонки Type/Model/Name/Max parallel runs/API key/Enabled), пикер агента «type — model (name)», disabled-профили видимы но не выбираемы | Потыкать Settings-страницу |
| 11 | Внедрение: перенос агентов | **Шаг 0 — живой acceptance** (перенесено из 3/4/6 решением 2026-07-13): quickstart-сценарии C–E фичи 006 на реальном тикете — агент через UI + test-run на mock (гейты T158/T070), первый живой `claude_cli`-прогон (T091), callback-прогон с request_human→queue→resume (T117); журнальные записи T071/T092/T118/T159 + галка T055 одним коммитом. Затем: 6 боевых агентов по одному переезжают с Jira Automation на готовый продукт (создание через UI); отключение Jira Automation; критерий: боль №1 закрыта | Тикет «не жалко» + маппинг статусов; промпты агентов |
| 12 | Executor `anthropic_api` | Второй транспорт (реш. 2026-07-16): Agent SDK `query()` **in-process в worker'е** — без спауна CLI, без CLI-бинаря в образе и login-состояния; запросы идут напрямую в API Anthropic. Новая ветка discriminated union в contracts (`api_key` **обязателен** — фолбэка на подписку у API-транспорта нет; model, max_turns, max_parallel_runs), очередь `queue:anthropic_api`, structured report — in-process SDK MCP `complete_task` + `outputFormat` fallback, `permissionMode:'dontAsk'` + `allowedTools`, rate-limit по SDK-стриму/429 → `worker.rateLimit`, healthCheck — валидность ключа. Переиспользуется всё из ит. 10: AES-256-GCM-хранение ключа, `has_api_key`, per-profile gate, форма профиля; плюс worktree-подготовка и маппинг `exitStatus`→`runs.status` из architecture §4. Ключевой мотив: чистый серверный BYOK-транспорт + фундамент для `deepseek_api` (~80% общего кода) | Одобрить план; API-ключ Anthropic |
| 13 | Executor `deepseek_api` | Дешёвый тир для массовых/рутинных тикетов (реш. 2026-07-16: второй провайдер — DeepSeek, не OpenAI. Мотивы: официальный Anthropic-совместимый endpoint → минимальный дифф поверх ит. 12; цена на порядок ниже (V4 Pro $0.435/$0.87 за 1M vs $5/$25–30 у Claude/GPT); BYOK без юр-нюансов. OpenAI отклонён: паритет цены с уже имеющимся Claude — не даёт нового тира, а интеграция — полностью новый цикл; вернуться к нему, если понадобится failover уровня Opus). **Шаг 0 — спайк из §4**: Agent SDK + `ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic`, проверить поведение `cache_control` (молчаливая деградация на автокэш DeepSeek или ошибка). План A: параметризация транспорта ит. 12 (base URL + модель; актуальные id — `deepseek-v4-pro`/`deepseek-v4-flash`, legacy-алиасы `deepseek-chat`/`deepseek-reasoner` отключаются 2026-07-24 — не использовать); план B (спайк провалился): свой OpenAI-совместимый цикл. Policy-деградации из §4 обязательны: строгие tool-схемы, forced tool call `complete_task` + repair-loop, недоверие self-report | Одобрить план; DeepSeek API-ключ |
| 14 | Workspace setup оркестратором ("Generate agents") | **Выполнено 2026-07-16** (spec-kit фича 011, PR #9): новый workspace создаётся НА ПАУЗЕ; кнопка Generate agents (пока в workspace нет worker-агентов) запускает бестикетный setup-прогон brigadir'а (`trigger source workspace-setup`, миграция 0005: nullable `ticket_id` + partial-индекс `runs_one_active_setup`); оркестратор изучает проект через **read-only Jira-тулзы** (`get_project_overview`/`search_tickets`/`get_ticket` — доступны ВСЕМ callback-прогонам, JQL компонует сервер, креды агенту не выдаются) и возвращает one-shot **`team`-outcome**; accept-path валидирует proposal атомарно (lintAgent против живой борды, executor-профили по имени) и одной транзакцией создаёт агентов enabled + бестикетную review-задачу; невалидный proposal — 422 repair-loop внутри прогона; единственный гейт — существующий Start. Resume запаркованного setup-прогона — новый setup-прогон с Q&A (не answer-triage) | Потыкать: создать workspace → Generate agents → ревью команды → Start |
| 15 | Админский MCP «Claude-тимлид» (`brigadir-admin`) | **Выполнено 2026-07-16** (spec-kit фича 012): stdio MCP-сервер `brigadir-admin` (`packages/admin-mcp`) — тонкий HTTP-клиент админского API, подключается к Claude Code; человек+Клод СНАРУЖИ прогона собирают команду (recon борды → создание workspace → спавн агентов). 9 тулз (5 read: `list_workspaces`/`get_workspace`/`get_board_statuses`/`list_executors`/`list_agents`; 4 write: `create_workspace` — workspace всегда PAUSED, без Start-тулзы; `generate_agents`; `create_team` — атомарный спавн; `create_agent`/`update_agent`), каждая с `inputSchema` И `outputSchema` (zod 4 в contracts, `z.toJSONSchema` draft-07). Секреты только из env сервера (bearer в заголовок, Jira-креды в тело create — модель их не видит, Принцип V); 4xx → tool-error с телом (path-qualified issues), 5xx → ретраи. Новый endpoint `POST /api/workspaces/:id/team` (DashboardTokenGuard) переиспользует `SetupApplyService` (рефактор: `validateTeam`+`insertTeamAgents` вынесены из run-bound пути, поведение feature 011 не изменено), all-or-nothing → 422; предусловие 409 `worker_agents_exist`. Никаких Jira-записей (Принцип III). Границы v1: без start/pause, без удаления, без пересборки команды | Подключить к Claude Code; собрать команду под борду |
| 16 | Варианты ответа на human-tasks («кнопки») | **Выполнено 2026-07-16** (spec-kit фича 013): агент может приложить к вопросу до 5 предопределённых вариантов ответа — `options: [{label ≤80, value? ≤500, description? ≤200}]` (одна схема `AnswerOptionSchema` в contracts, `.strict()`, value default = label при клике) на ОБЕИХ ask-поверхностях: `request_human` И `human_task` в `needs_human`-отчёте. Хранение — nullable `human_tasks.options` jsonb (миграция 0006 + REVIEW; у системных задач NULL); label/value/description проходят скраббер как title/details (Принцип V); `HumanQueueItem.options` отдаётся глобальным списком и workspace-табом. В drawer'е — кнопки над полем ответа: клик ПОДСТАВЛЯЕТ value в textarea (без автосабмита, explicit Submit как был), free text всегда доступен; ответ остаётся обычной строкой через существующий resolve — **resume/answer-triage не тронуты по построению**. Jira-зеркало: plain-список «Suggested answers» в ADF-комменте вопроса (кнопок в Jira нет). Агенты узнают о поле из одного предложения в `callbackToolsSection` + подсказки в setup-handoff'е — stored-инструкции не редактируются. Mock-executor: `trigger_event.mock_options`. Границы v1: без multi-select, без валидации answer∈options, без интерактива в Jira, без редактирования options | Потыкать: вопрос с вариантами → кнопка → Submit |
| 17+ | По желанию | `claude_routines` executor, Slack-уведомления о human-tasks, статистика, `--resume` вместо нового прогона при ответе человека, **Триггеры v2** (идея 2026-07-14): Jira webhook (эндпоинт → дедуп по `webhook_events` — таблица и 1-й уровень дедупа ждут с итерации 1 → тот же `onStatusChanged`; поллер остаётся reconciliation-страховкой) + cron/по-времени (repeatable-job как у reconcile; агенту вместо статуса — JQL-запрос, тикетная модель прогона не меняется) + внешний API (фактически уже есть: manual-trigger с bearer). Единственный рефактор при первом втором типе триггера: `agents.trigger_status` → дискриминированный `trigger` jsonb `{type: status\|webhook\|cron\|api, ...}`, **транскрипт-вьювер прогона** (идея 2026-07-14): tee stream-json в файл (gzip, retention, скраббер секретов из ит.4, кап размера) + `GET /api/runs/:id/transcript` + вкладка Transcript в карточке с live-tail запущенного прогона; **report→field маппинг** (идея 2026-07-14, нужен для переноса Planner): агент кладёт план в отчёт, система пишет его в поле тикета (напр. customfield_10270) через write-queue; **repository: none** у агента — прогон без worktree для чисто-плановых задач | — |

> Порядок изменён 2026-07-11 (решение пользователя): сначала строим продукт до полной функциональности (callbacks → UI), внедрение боевых агентов — финальным шагом на готовом продукте, один раз. Обратная сторона принята осознанно: боль №1 (ненадёжная Jira Automation) живёт дольше, а обёртка/executor до внедрения обкатываются только на live-smoke и тестовых прогонах.

## Модель «временная команда под фичу» (анализ 2026-07-11)

Workspace + agents + scope_jql = временная команда digital employees на узкий список задач. Ядро дизайна модель поддерживает; выявленные пробелы и их размещение:

| Пробел | Решение | Итерация |
|---|---|---|
| Зависимости тикетов (frontend ждёт backend) | **Dependency gate**: триггер не срабатывает при незакрытых `is blocked by`; reconcile перепроверяет пропущенные (закрытие блокера не обновляет `updated`) — см. spec §0.4 | 2 (gate) |
| Контекст между агентами фичи | Обёртка включает эпик + linked issues со статусами/ветками/PR из отчётов прошлых прогонов | 4 (callbacks/обёртка) |
| Пауза/архив временной команды | `workspaces.enabled` — выключатель поллера с сохранением истории | 5 (UI) |
| Бюджет команды | Потолок/алерт стоимости на workspace (поверх per-run) | 6 (UI/stats) |
| Шаблоны команд | Экспорт/импорт набора агентов (yaml уже есть как основа) | 12+ |
| Финиш фичи = merge PR'ов | `code_delivery=pull_request` → non-blocking human task `kind=review`; merge — за человеком | 4 (callbacks) |

Не делаем осознанно: агент-декомпозитор (эпик→тикеты), мульти-репо в одном прогоне (split на тикеты + dependency gate покрывают кейс).

### Пост-MVP: админский MCP-сервер «Claude-тимлид» (идея 2026-07-11, итерация 12+)

Stdio MCP-сервер поверх админского API платформы, подключаемый к Claude Code. Сценарий: пользователь даёт Клоду ссылки на репозитории и Jira-борду и просит «собери команду под эти задачи»; Клод как тимлид смотрит задачи на борде, создаёт workspace и собирает в нём агентов под конкретный набор задач (триггер-статусы, инструкции, scope_jql).

- **Тулзы (эскиз)**: `inspect_board` (тип борды, статусы, задачи со статусами/линками — read-only разведка), `create_workspace` (борда + репозитории + scope_jql — та же валидация, что у визарда), `create_agent` / `update_agent`, `list_workspaces`, `export_team` / `import_team` (шаблоны команд из строки выше — yaml как формат обмена).
- **Не путать с callback-MCP итерации 4**: тот — для агентов ВНУТРИ прогона (report_progress/request_human/complete_task, run-JWT, минимальные права). Этот — админская плоскость для человека+Клода СНАРУЖИ (создание команд), авторизация — общий bearer дашборда.
- **Ничего не изобретаем заранее**: MCP-сервер — тонкая обёртка над теми же endpoint'ами, что использует UI итерации 5 (workspace wizard, agent CRUD). Предпосылки: итерация 5 (API создания workspace/агентов) + шаблоны команд (12+). «Клод-тимлид» не запускает прогоны и не двигает тикеты — он только собирает команду; исполнение остаётся за пайплайном.
- Дальний прицел: тот же сервер — точка входа для «декомпозитора» (эпик→тикеты), от которого мы пока осознанно отказались; если когда-нибудь вернёмся — это будет тулза здесь, а не агент внутри пайплайна.

### Пост-MVP: workspace-оркестратор и planning-режим (идея 2026-07-13, итерация 12+; развитие «Claude-тимлида»)

Эволюция «тимлида» из внешней Claude Code-сессии в первоклассную сущность продукта — **оркестратор workspace**, зеркалящий то, как сейчас работает пара «пользователь + Клод-координатор» над самим BRIGADIR'ом.

- **Planning-режим (стартовая ценность)**: workspace получает состояния `draft → planning → ready → running`. Пользователь готовит workspace/борду, пишет targets на спринт и запускает planning. Оркестратор: (1) read-only проходит по всем тикетам борды и понимает скоуп; (2) составляет план команды и **создаёт нужных агентов выключенными** (`enabled=false`) — триггер-статусы, инструкции, executor'ы; (3) завершает human-task'ой `kind=review` «команда собрана — проверьте». Пользователь ревьюит агентов в существующем UI и жмёт Start → workspace `running`. Approval gate — существующая механика human-queue, ничего нового.
- **Трекинг и коррекции (вторая фаза)**: оркестратору НЕ нужен отдельный канал репортов — вся телеметрия уже в Postgres (`runs`, `run_events`, `run_checks`, `human_tasks`). Коррекции — не постоянно висящая сессия (дорого), а периодические/событийные прогоны оркестратора (например, повторные фейлы агента → оркестратор просыпается, читает историю, правит инструкцию агента). Право записи — только в `agents`/`workspace settings`.
- **Инварианты (не нарушать)**: оркестратор **не запускает прогоны** (только пайплайн через статусы Jira — правило «агенты друг друга не запускают» остаётся), **не пишет в Jira** (Принцип III), его правки агентов проходят тот же валидационный путь, что и UI/API.
- **Путь внедрения**: сначала админский MCP выше (те же тулзы, человек в цикле как оркестратор) — обкатка сценария вручную; потом planning-режим как автоматизация ровно этого сценария. Общая предпосылка: executors/agents CRUD (готово в 6), шаблоны команд (12+).

Критерии готовности итераций 1–4 — DoD Phase 0 из [roadmap.md](roadmap.md), итераций 5–6 — DoD Phase 1; итерация 11 — внедрение (критерий: все 6 агентов на BRIGADIR, Jira Automation отключена). Детальная спека — [spec.md](spec.md) §0.x–1.x с поправками из этого файла.

## Модель для команды

- Оркестратор — на общей машине/VPS в своей инфре; Jira — выделенный бот-аккаунт (действия агентов атрибутируются боту).
- Исполнение: старт — подписка Max владельца на его машине (личный пайплайн); для общего командного раннера — командный API-ключ Anthropic со spend cap (~$0.5–2/прогон на Sonnet 5), транспорт — executor `anthropic_api` (итерация 12); массовые/рутинные тикеты — дешёвый тир `deepseek_api` (итерация 13).
